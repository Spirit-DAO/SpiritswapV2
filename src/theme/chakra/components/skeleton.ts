import { borderRadius } from 'theme/base/borderRadius';

const Skeleton = {
  baseStyle: {},

  sizes: {},

  variants: {
    default: {
      borderRadius: borderRadius.sm,
    },
  },

  defaultProps: {
    variant: 'default',
  },
};

export default Skeleton;
