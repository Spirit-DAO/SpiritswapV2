const translationPath = 'suggestions.content';

export const SWAP_TEXTS = {
  1: `${translationPath}.swap.1`,
  2: `${translationPath}.swap.2`,
  3: `${translationPath}.swap.3`,
};

export const BRIDGE_TEXTS = {
  1: `${translationPath}.bridge.1`,
  2: `${translationPath}.bridge.2`,
  3: `${translationPath}.bridge.3`,
};

export const LIQUIDITY_TEXTS = {
  1: `${translationPath}.liquidity.1`,
  2: `${translationPath}.liquidity.2`,
};

export const INSPIRIT_TEXTS = {
  1: `${translationPath}.inSpirit.1`,
  2: `${translationPath}.inSpirit.2`,
};

export const FARMS_TEXTS = {
  1: `${translationPath}.farms.1`,
  2: `${translationPath}.farms.2`,
  3: `${translationPath}.farms.3`,
};

export const DISCLAIMER_TEXTS = {
  1: `${translationPath}.disclaimer.1`,
  2: `${translationPath}.disclaimer.2`,
};
